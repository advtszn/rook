---
name: conventional-commits
description: User requires conventional commit messages (feat/fix/chore/refactor etc)
metadata:
  type: feedback
---

Always use conventional commit format for commit messages (e.g. `feat:`, `fix:`, `chore:`, `refactor:`).

**Why:** User explicitly corrected a generic commit message — strong preference.

**How to apply:** Every commit message must start with a conventional commit type prefix.
